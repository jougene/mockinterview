const routes = {
  root: require('./root.controller'),
  my: require('./my.controller'),
  interview: require('./interview.controller'),
  user: require('./user.controller')
}

module.exports = routes
